#include <iostream>

// Assignment 3 Tests

bool isempty()
{
    if(front == NULL && rear = NULL)
    return true;
    else
    {
      return false; 
    }
    
}

// tests 

int test()
{
    if (isempty('front = NULL && rear = NULL') != true) return 1;
    if (isempty('front == 0> && rear = 0>') != false) return 2;
 
    return 0;
}
 
int main()
{
    return 0;
}